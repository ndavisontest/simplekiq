source 'https://rubygems.org'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

source 'https://gem.fury.io/chime/' do
  gem 'chime-atlas', '>= 3.3.6'
end

# Specify your gem's dependencies in simplekiq.gemspec
gemspec
